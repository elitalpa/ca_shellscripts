#!/bin/bash
echo "Hello, World!"

# This line is a comment
#
# The first line of the shell script file begins with a "sha-bang" (#!) which is not read as a comment,
# followed by the full path where the shell interpreter is located.
#
# Useful Commands :
# ps | grep $$ # find out currently active shell
# which bash # find out shell interpreter full path